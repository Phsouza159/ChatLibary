const express = require('express')
    , router = express.Router()

const clientsContoller = (function(app) { 

    router.get('/' , (req, res) => {
        
    })

})

module.exports = clientsContoller